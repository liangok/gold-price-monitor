name: goldprice_domain
description: 金价监控 App 的纯 Dart 领域层：数据模型、技术指标、策略引擎、渠道计算。
  不依赖 Flutter，也不依赖任何第三方包，因此可以用 dart run tool/verify.dart 离线验证。
  算法必须与 Python 参考实现 analysis/indicators.py 语义一致。
version: 0.1.0
publish_to: none
environment:
  sdk: ">=3.5.0 <4.0.0"
